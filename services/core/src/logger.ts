/**
 * @deprecated Import from `./shared/logger` directly in new code.
 * Kept for backward-compatible imports within the core service.
 */
export { logger as default, logger, LoggerManager } from './shared/logger';
