# Deprecated — use backend-deploy.yml (auto-detects changes).
# Kept for manual one-off runs only.

name: Build & Deploy Admin API (manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/admin-api-deploy.yml
    secrets:
      SSH_USER: ${{ secrets.SSH_USER }}
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
      DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
      DEPLOY_REPO_PATH: ${{ secrets.DEPLOY_REPO_PATH }}
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
