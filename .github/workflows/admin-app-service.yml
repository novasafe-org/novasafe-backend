# Manual one-off — delegates to the full Admin API pipeline.
# For everyday use: push to main or run "NovaSafe Backend Deploy".

name: Build & Deploy Admin API (manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/admin-api-deploy.yml
    secrets: inherit
