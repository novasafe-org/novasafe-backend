# Manual one-off — delegates to the full Mobile API pipeline.
# For everyday use: push to main or run "NovaSafe Backend Deploy".

name: Build & Deploy Mobile API (manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/mobile-api-deploy.yml
    secrets: inherit
