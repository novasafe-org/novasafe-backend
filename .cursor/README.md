# Cursor project context

| File | Purpose |
|------|---------|
| [SESSION_CONTEXT.md](./SESSION_CONTEXT.md) | Full handoff summary of the NovaSafe core + api-playground build session |

**Usage:** In a new Cursor chat on another machine:

```text
Read .cursor/SESSION_CONTEXT.md and continue from there.
```

Chat transcripts are **not** stored here. They live under:

```text
~/.cursor/projects/<workspace-hash>/agent-transcripts/
```

This folder is safe to commit (no secrets).
